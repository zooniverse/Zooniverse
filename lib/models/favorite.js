// Generated by CoffeeScript 1.3.3
(function() {
  var Api, Favorite, ProfileItem, User,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Api = require('../api');

  ProfileItem = require('./profile_item');

  User = require('./user');

  Favorite = (function(_super) {

    __extends(Favorite, _super);

    Favorite.configure("Favorite", "project_id", "workflow_id", "subjects", "created_at");

    function Favorite() {
      this.unfavorite = __bind(this.unfavorite, this);

      this.send = __bind(this.send, this);

      this.toJSON = __bind(this.toJSON, this);
      Favorite.__super__.constructor.apply(this, arguments);
    }

    Favorite.url = function(params) {
      return Favorite.withParams("/projects/" + User.project + "/users/" + User.current.id + "/favorites", params);
    };

    Favorite.fetch = function() {
      if (User.current) {
        return Favorite.__super__.constructor.fetch.apply(this, arguments);
      }
    };

    Favorite.fromJSON = function(results) {
      var item, _i, _len, _results;
      _results = [];
      for (_i = 0, _len = results.length; _i < _len; _i++) {
        item = results[_i];
        _results.push(Favorite.create(item));
      }
      return _results;
    };

    Favorite.prototype.toJSON = function() {
      return {
        favorite: {
          subject_ids: [this.subjects.id]
        }
      };
    };

    Favorite.prototype.send = function() {
      var _this = this;
      return Api.post("/projects/" + User.project + "/favorites", this.toJSON(), function(response) {
        _this.id = response.id;
        return _this.save();
      });
    };

    Favorite.prototype.unfavorite = function() {
      var _this = this;
      return Api["delete"]("/projects/" + User.project + "/favorites/" + this.id, function() {
        return _this.destroy();
      });
    };

    return Favorite;

  }).call(this, ProfileItem);

  module.exports = Favorite;

}).call(this);
