// Generated by CoffeeScript 1.3.3
(function() {
  var Api, Model, ProxyFrame, User,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Model = require('./model');

  Api = require('../api');

  ProxyFrame = require('../proxy_frame');

  User = (function(_super) {

    __extends(User, _super);

    function User() {
      return User.__super__.constructor.apply(this, arguments);
    }

    User.configure('User');

    User.project = 'Not Specified';

    User.fetch = function() {
      var fetcher,
        _this = this;
      fetcher = Api.getJSON("/projects/" + this.project + "/current_user", this.createUser);
      fetcher.always(this.setAuthHeaders);
      fetcher.always(function() {
        return User.trigger('sign-in');
      });
      return fetcher;
    };

    User.setAuthHeaders = function() {
      var auth;
      if (User.current) {
        auth = base64.encode("" + User.current.name + ":" + User.current.api_key);
        return ProxyFrame.headers['Authorization'] = "Basic " + auth;
      } else {
        return delete ProxyFrame.headers['Authorization'];
      }
    };

    User.login = function(_arg) {
      var login, password, username,
        _this = this;
      username = _arg.username, password = _arg.password;
      login = Api.getJSON("/projects/" + this.project + "/login", {
        username: username,
        password: password
      }, this.createUser);
      login.always(this.setAuthHeaders);
      login.always(function() {
        return User.trigger('sign-in');
      });
      return login;
    };

    User.logout = function() {
      var logout,
        _this = this;
      logout = Api.getJSON("/projects/" + this.project + "/logout", function(result) {
        if (result.success) {
          return User.current = null;
        }
      });
      logout.always(this.setAuthHeaders);
      logout.always(function() {
        return User.trigger('sign-in');
      });
      return logout;
    };

    User.signup = function(_arg) {
      var email, password, signup, username,
        _this = this;
      username = _arg.username, password = _arg.password, email = _arg.email;
      signup = Api.getJSON("/projects/" + this.project + "/signup", {
        username: username,
        email: email,
        password: password
      }, this.createUser);
      signup.always(this.setAuthHeaders);
      signup.always(function() {
        return User.trigger('sign-in');
      });
      return signup;
    };

    User.createUser = function(result) {
      return User.current = result.success ? (delete result.success, new User(result)) : (User.trigger('sign-in-error', result.message), null);
    };

    return User;

  })(Model);

  module.exports = User;

}).call(this);
